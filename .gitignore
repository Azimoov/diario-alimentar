# configurações locais do Claude Code — não publicar
.claude/

# dependências e artefatos do proxy (Fase 2)
# (as duas formas: com barra pega a pasta, sem barra pega symlink de teste)
node_modules/
node_modules
.wrangler/
.dev.vars

# fontes brutas grandes (baixe de novo com as URLs do README)
data/source-tbca/
data/source-usda/

# documentos clínicos PESSOAIS convertidos p/ leitura local.
# O repositório é PÚBLICO: laudo, diagnóstico e medicação de alguém não entram
# aqui. Fica na pasta local de quem usa; o que é generalizável já está em
# fase2-proxy/src/conhecimento.js, sem dado individual.
referencias-pessoais/

# artefatos dos testes de navegador (screenshots gravados no cwd)
shot-*.png
previa-*.png
card*.png
hist.png
export-teste.zip
