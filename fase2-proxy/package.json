{
  "name": "diario-alimentar-proxy",
  "private": true,
  "type": "module",
  "description": "Proxy serverless (Cloudflare Worker) da Fase 2 — guarda a chave da API e analisa fotos de refeicoes. A chave NUNCA vai para o front-end.",
  "scripts": {
    "dev": "wrangler dev",
    "dev:local": "node dev-server.mjs",
    "deploy": "wrangler deploy",
    "test": "node test/smoke.mjs && node test/import-saude.mjs && node test/openbrain.mjs",
    "test:openbrain": "node test/openbrain.mjs",
    "test:saude": "node test/import-saude.mjs",
    "test:ia": "node test/area-ia.mjs"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.110.0"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
